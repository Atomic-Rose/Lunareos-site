---
import { SITE, FOOTER_LINKS, withBase } from '../site'

const year = new Date().getFullYear()
---

<footer class="site-footer">
  <div class="shell">
    <div class="site-footer__top">
      <div>
        <a class="site-footer__brand" href={withBase('/')} aria-label="Lunareos — home">
          <img
            src={withBase('/lunareos-wordmark-silver.svg')}
            alt="Lunareos"
            width="1174"
            height="174"
          />
        </a>
      </div>

      <div class="site-footer__cols">
        <div class="site-footer__col">
          <p class="label">Pages</p>
          <ul>
            {FOOTER_LINKS.map((link) => <li><a href={link.href}>{link.label}</a></li>)}
          </ul>
        </div>

        <div class="site-footer__col">
          <p class="label">Say hello</p>
          <ul>
            <li><a href={`mailto:${SITE.email}`}>{SITE.email}</a></li>
          </ul>
        </div>
      </div>
    </div>

    <div class="site-footer__bottom">
      <p class="label">{SITE.legalName}</p>
      <p class="label">© {year} {SITE.legalName}</p>
    </div>
  </div>
</footer>
